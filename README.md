# edtech — Urdu Edtech Platform

Full-stack web platform for teaching the Urdu programming language in schools.

## Setup

```bash
npm install
cp .env.example .env.local   # fill in values
npm run db:push
npm run db:seed
npm run dev
```

See the root [README](../README.md) for full documentation.
