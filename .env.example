DATABASE_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres"
NEXTAUTH_SECRET="replace-with-a-random-secret"
NEXTAUTH_URL="https://app.nuqta.dev"
ANTHROPIC_API_KEY="sk-ant-..."
NUQTA_API_URL="https://api.nuqta.dev"
# Shared password gate in front of everything except the demo/test-run login
# (see src/middleware.ts). Give this out-of-band to onboarded pilot schools —
# it is separate from each user's individual login password.
PILOT_GATE_PASSWORD="replace-with-a-shared-pilot-password"
